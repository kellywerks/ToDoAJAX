<%= form_for task do |f| %>
  <%= f.label task.name %>
  <%= link_to "update", task_path(task), :class => "btn-xs btn-warning", :method => "patch" %>
  <%= link_to "remove", task_path(task), :class => "btn-xs btn-danger", method: :delete, data: {confirm: "You sure?"}, remote: true %>
<% end %>
